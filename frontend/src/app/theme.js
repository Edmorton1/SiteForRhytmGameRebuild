import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
	palette: {
		mode: 'dark',
		primary: {
			main: '#4F378A',
			transparent: 'rgba(79, 55, 138, 0.75)',
		},
		background: { default: '#49454F' },
	},
});
