import { FormDataJsonValidationPipePipe } from './form-data-json-validation-pipe.pipe';

describe('FormDataJsonValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new FormDataJsonValidationPipePipe()).toBeDefined();
  });
});
